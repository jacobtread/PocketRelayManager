import { FunctionComponent } from "react";
import { useAuth } from "../components/AuthProvider";

const GamesRoute: FunctionComponent = () => {
    return (
        <div>
            <h1>Games</h1>
        </div>
    )
}

export default GamesRoute